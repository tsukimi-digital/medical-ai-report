ANTHROPIC_API_KEY=...
OPENAI_API_KEY=...
# REQUIRED — must be set before starting the server; absence will throw at runtime.
# Generate a secure random value, e.g.: openssl rand -hex 32
AUTH_SECRET=
AI_PIPELINE_ADVANCED=false
